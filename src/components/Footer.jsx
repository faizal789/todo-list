import React from "react";

const Footer = () => {
  return (
    <footer className="mt-auto">
      <span className="cursor-default">Drag and drop to reorder list</span>
    </footer>
  );
};

export default Footer;
